# ✅ Локалізація назв послуг в сповіщеннях

## Що було виправлено:

### 🔧 **Проблема:**
В сповіщеннях про завершення сесії та зміну статусу показувалась нелокалізована назва послуги (завжди українською).

### 💡 **Рішення:**

1. **Додано метод `_getLocalizedService`** в NotificationService:
```dart
String _getLocalizedService(String service) {
  if (_languageProvider == null) return service;
  
  switch (service) {
    case 'Манікюр класичний':
      return _languageProvider!.getText('Манікюр класичний', 'Маникюр классический');
    case 'Покриття гель-лак (руки)':
      return _languageProvider!.getText('Покриття гель-лак (руки)', 'Покрытие гель-лак (руки)');
    // ... всі інші послуги
    default:
      return service;
  }
}
```

2. **Використання в сповіщеннях:**
```dart
// В _showSessionEndNotification:
final localizedService = _getLocalizedService(session.service);
final body = '${session.clientName} - $localizedService\n...';

// В _showAutoMissedNotification:
final localizedService = _getLocalizedService(session.service);
final body = '${session.clientName} - $localizedService\n...';
```

### ✅ **Результат:**

#### **Українська мова:**
- `Манікюр класичний` → показується як є
- `Педикюр класичний` → показується як є

#### **Російська мова:**
- `Манікюр класичний` → `Маникюр классический`
- `Педикюр класичний` → `Педикюр классический`

## 📱 **Типи сповіщень з локалізацією:**

1. **Завершення сесії:**
   ```
   ⏰ Сеанс завершен
   Марія Іванова - Маникюр классический
   Мастерица: Анна
   Пожалуйста, обновите статус записи
   ```

2. **Автоматична зміна статусу:**
   ```
   🔴 Статус записи: "Пропущено"
   Марія Іванова - Маникюр классический
   Мастерица: Анна
   Статус изменен на "Пропущено"
   ```

## 🛡️ **Додаткові покращення:**
- Додана підтримка опечатки `Педикюр класиний` (без 'ч')
- Fallback на оригінальну назву для невідомих послуг
- Перевірка наявності _languageProvider перед локалізацією

**Тепер всі сповіщення повністю локалізовані! 🎉**